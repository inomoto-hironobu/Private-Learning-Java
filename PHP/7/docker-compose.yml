version: "2"
services:
  web:
    build: .
    ports:
     - "80:80"
    volumes:
     - ./src:/var/www/html
  db:
    build: ./db/
    ports:
     - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
